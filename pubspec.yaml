name: filesaverplus
description: "FileSaverPlus is a Flutter plugin that allows you to easily save files to the device’s local storage, providing cross-platform support for Android and iOS. With this plugin, you can save text, images, or any file data to a specified directory on the device. It also handles file permissions and ensures a seamless experience for saving files in a secure and efficient way."
version: 0.0.1
repository: https://github.com/devamitkumartiwari/filesaverplus.git

homepage:

environment:
  sdk: '>=3.7.2 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  plugin:
    platforms:
      android:
        package: com.amit.filesaverplus
        pluginClass: FilesaverplusPlugin
      ios:
        pluginClass: FilesaverplusPlugin
